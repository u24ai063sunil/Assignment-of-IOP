// C program to do sum of two numbers
#include <stdio.h>
int main() 
{
    int a,b,sum;
    printf("enter your numbers a,b");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("total sum is=%d",sum);
 }
